# Akron Documentation Review Log — Pass 2

## 2026-05-26 19:21:00
- Initialized the Pass 2 documentation review tracker.
- Reviewed `docs/akr-packs/import-export.mdx`:
  - Verified import/export steps and scoping.
  - Confirmed the scope values (`Whole`, `StartPos`, `Keybinds`, `AutoKill`, `AutoDeafen`, `Recorder`, `Audio`, `Hud`) match the actual C# `AkronProfileSection` enum definition.
  - Confirmed the section behavior description table is accurate and matches how settings are stored/applied.
  - Checked the internal reference link `/reference/akr-archives` and image `/images/akr-export-scope.png` (both exist and are valid).
  - No changes needed; marked as reviewed.
- Reviewed `docs/akr-packs/manage-setups.mdx`:
  - Confirmed profile definitions (`Casual`, `Practice`, `Leaderboard Clean`, `Sandbox`, `Map Maker`, `Accessibility`) match the actual C# `AkronProfile` enum values (`Casual`, `Practice`, `LeaderboardClean`, `Sandbox`, `MapMaker`, `Accessibility`).
  - Checked the internal concept link `/concepts/clean-vs-cheat` (exists and is valid).
  - Verified accuracy of workflows and maintenance steps.
  - No changes needed; marked as reviewed.

## 2026-05-26 19:32:00
- Reviewed `docs/concepts/akr-files-and-setup-state.mdx`:
  - Verified explanation of Akron setup state, profile packs, and scoped sections.
  - Checked the internal links `/akr-packs/import-export` and `/reference/akr-archives` (both exist and are valid).
  - Confirmed ZIP manifest (`manifest.json`) and payload (`profile.json`) entry names match C# source definitions in `akron-archive.cs` and `akron-profile-packs.cs`.
  - No changes needed; marked as reviewed.
- Reviewed `docs/concepts/clean-vs-cheat.mdx`:
  - Verified attempt classifications (`Goldberry/Hardlist clear`, `Normal clear`, and `Cheat`) match C# source values (`GoldberryHardlistClean`, `RegularClean`, `Cheat`) and formatting/naming in `akron-module-settings-display.cs`.
  - Checked internal link to `/feature-guide` (exists and is valid).
  - Ensured table formatting and classifications align perfectly with registry definitions in `AkronFeatureRegistry.cs`.
  - No changes needed; marked as reviewed.
- Reviewed `docs/concepts/proof-and-submission-mode.mdx`:
  - Verified listed proof metadata helpers (`Proof Recorder Guard`, `End Screen Helper`, `Pause Tracker`, `Map Version Stamp`, `Golden Start`, `Lag Pauser`, `Journal Snapshot / Compare`) and classifications align with registry definitions in `AkronFeatureRegistry.cs`.
  - Checked internal link `/rulesets/streamer-mode-and-proof-overlays` (exists and is valid).
  - No changes needed; marked as reviewed.

## 2026-05-26 19:43:00
- Reviewed `docs/concepts/rulesets-and-status.mdx`:
  - Verified ruleset categories (`Casual`, `Practice`, `Leaderboard-clean`, `Sandbox`, `Everest-safe`, `Map-maker`) and status levels (`Goldberry/Hardlist clean`, `Regular clean`, `Cheat`) match actual C# `PrimaryRuleset` and `AkronStatus` enum declarations.
  - Confirmed the common feature classifications (Input Display, Room Labels, Audio Speed/Pitch, StartPos Restore, Hitbox Viewer, FPS/TPS Bypass) match implementation defaults registered in `AkronFeatureRegistry.cs`.
  - Checked the internal links `/rulesets/overview`, `/concepts/clean-vs-cheat`, and `/feature-guide` (all exist and are valid).
  - No changes needed; marked as reviewed.
- Reviewed `docs/contributing/contribution-workflow.mdx`:
  - Verified "Files By Concern" table is accurate and matches current namespace structures and file locations in the `Source/` and `tests/` directories.
  - Confirmed the review/merge checklist, AI disclosure requirements, and human verification expectations align with target repositories.
  - No changes needed; marked as reviewed.
- Reviewed `docs/contributing/development-setup.mdx`:
  - Verified development build, format, and test command instructions are accurate.
  - Confirmed referenced test filters (`FeatureRegistryTests`, `ModuleSettingsTests`, `ArchiveTests`) exist in `/tests` directory files.
  - Checked internal links `/contributing/testing-and-verification` and `/contributing/public-architecture` (both exist and are valid).
  - No changes needed; marked as reviewed.

## 2026-05-26 19:54:00
- Reviewed `docs/contributing/feature-policy.mdx`:
  - Confirmed classifications match `Source/Core/AkronFeatureRegistry.cs` and `Source/Core/AkronTypes.cs` perfectly.
  - Verified default test command and references exist and are valid.
  - Checked absolute-style concepts links, confirming they are valid and follow repository standards.
  - No changes needed; marked as reviewed.
- Reviewed `docs/contributing/implementation-boundaries.mdx`:
  - Verified source file structures and feature families across settings, module, HUD, saver/loader, commands, overlay, and tests components.
  - Confirmed all listed `.cs` source partials exist exactly as named in `Source/`.
  - No changes needed; marked as reviewed.
- Reviewed `docs/contributing/public-architecture.mdx`:
  - Confirmed core boundaries, responsibilities, policy calls, and system shape align with the active codebase.
  - Verified reference links are valid and active.
  - No changes needed; marked as reviewed.
- Reviewed `docs/contributing/testing-and-verification.mdx`:
  - Checked command filters, live validation constraints, pull request guidelines, and test standards.
  - No changes needed; marked as reviewed.
- Reviewed `docs/docs.mdx` & `docs/index.mdx`:
  - Confirmed all listed link targets and card structures resolve to existing files.
  - No changes needed; marked as reviewed.

## 2026-05-26 20:05:00
- Reviewed `docs/feature-guide/bypass.mdx`:
  - Verified Bypass tab save/completion mutation options against registry definitions in `AkronFeatureRegistry.cs` and `akron-overlay-entries.cs`.
  - Confirmed attempt statuses (`Cheat`, `Regular clean`) map perfectly to codebase `AkronStatus` classifications and UI classifications.
  - No changes needed; marked as reviewed.
- Reviewed `docs/feature-guide/creator.mdx`:
  - Verified Creator tab map inspection, camera, room warp, and export options against actual options in `akron-overlay-entries.cs` and popup controls in `akron-overlay-visual-popups.cs` and `akron-overlay-runtime-popups.cs`.
  - Found that the suboption label for "Freeze timers" was outdated since the actual ImGui checkbox label is "Freeze time". Changed both occurrences to "Freeze time" to match the UI precisely.
  - Confirmed all other rows and attempt statuses are fully aligned.

## 2026-05-26 20:15:00
- Reviewed `docs/feature-guide/custom-labels.mdx`:
  - Verified Custom HUD labels options, template variables, expressions, placement, overlap modes, and event visibility against source code in `akron-label-commands.cs`, `akron-custom-hud-labels.cs`, and `akron-custom-hud-label-catalog.cs`.
  - Confirmed all listed template variables (`app_version`, `profile`, `ruleset`, `status`, `reason`, `chapter`, `map`, `room`, `player_x`, `player_y`, `speed`, `stamina`, `dashes`, `deaths`, `room_deaths`, `attempt`, `room_time`, `map_time`, `fps`, `tps`, `inputs`, `hazard_accuracy`, `hazard_invalid`, `savestate_slot`, `tas`, `speedrun_tool`, `clock`) match the `BuildVariables` dictionary in C# exactly.
  - Checked console command `akron_hud_labels` examples and confirmed they align with implemented command parsing options.
  - Verified internal reference links `/feature-guide/labels` and `/akr-packs/import-export` (both exist and are valid).
  - No changes needed; marked as reviewed.
- Reviewed `docs/feature-guide/external-tools.mdx`:
  - Verified external tool rows (Motion Smoothing, Speedrun Tool, CelesteTAS, Extended Variant Mode) and their options against C# sources in `akron-extended-variants.cs`, `AkronMotionSmoothingInterop.cs`, `MotionSmoothingSettings.cs`, and `akron-interop.cs`.
  - Confirmed and cross-referenced "Hide edge gaps" (mapped to `FrameBypassHideStretchedEdges` in C#) and all Extended Variant Mode randomizer options (reroll mode, display enabled variants, interval, max) with the UI and interop implementation.
  - Confirmed attempt statuses map correctly to registry classifications.
  - No changes needed; marked as reviewed.

## 2026-05-26 20:26:00
- Reviewed `docs/feature-guide/global.mdx`:
  - Identified missing documentation for `FPS Bypass` and `TPS Bypass` in the Global options list (both are classified as `Cheat` and enabled when the Motion Smoothing module is loaded).
  - Identified outdated documentation for `Autosave`'s options (referenced a stale "Backup behavior" menu option).
  - Added full Option and Option menu definitions for `FPS Bypass` (Target FPS, Method, Smooth Camera, Objects, TAS mode, Subpixel Madeline, Smooth background, Smooth foreground, Hide edge gaps, Silly mode).
  - Added full Option and Option menu definitions for `TPS Bypass` (Target TPS).
  - Documented the nine active sub-options for `Autosave` (Interval, Minimum delay, Room load, Spawn update, Respawn, Pause, Avoid gameplay, Save settings, Hide saving icon) to match `akron-overlay-runtime-popups.cs` perfectly.

## 2026-05-26 20:38:00
- Reviewed `docs/feature-guide/index.mdx`:
  - Verified index card paths against actual directory contents.
  - No changes needed; marked as reviewed.
- Reviewed `docs/feature-guide/interface.mdx`:
  - Identified inconsistency introduced in Pass 1 where all "Normal clear" classifications were renamed to "Regular clean". Restored them to "Normal clear" to match the actual Celeste Akron UI overlay display and format contract tested in `ModuleSettingsTests.FormatStatus`.
  - Identified stale "Community Packs" option menu rows. Because "Community Packs" does not expose a triangle options menu and instead opens a standalone browser, moved these rows to a dedicated "Community Packs Browser" section to match the source implementation in `akron-community-pack-browser.cs` and `module-settings-tests.cs`.

## 2026-05-26 20:50:00
- Reviewed `docs/feature-guide/internal-recorder.mdx`:
  - Verified FFmpeg-backed local internal recorder, replay buffer, completion clips, and proof helpers option rows and suboptions against actual options in `akron-overlay-entries.cs` and popup/choice controls in `akron-overlay-recorder-popups.cs`.
  - Identified that the main options table labeled the configuration field "End Screen Duration", whereas the actual option label in C# overlay code and registry is "Endscreen Duration" (one word). Corrected "End Screen Duration" to "Endscreen Duration".
  - Verified all other options and suboption rows map exactly to code definitions and have valid attempt classifications.
- Reviewed `docs/feature-guide/keybinds.mdx`:
  - Verified default keybind overview specs, popup actions, and custom keybind formats against `akron-overlay-keybind-entries.cs` and default action specs.
  - Confirmed sub-menu/popup options under Show Hitboxes and Cursor Zoom match controls drawn in `akron-overlay-runtime-popups.cs` and `akron-overlay-visual-popups.cs` perfectly.
  - No changes needed; marked as reviewed.

## 2026-05-26 21:00:30
- Reviewed `docs/feature-guide/labels.mdx`:
  - Verified HUD label visibility, room labels, status labels, input panels, counters, and custom labels option rows and suboptions against source files `akron-module-settings-hud.cs`, `akron-module-settings-label-rows.cs`, `akron-custom-hud-labels.cs`, and `akron-overlay-label-popups.cs`.
  - Confirmed option settings, menu controls, and attempt classifications (Regular clean, Goldberry/Hardlist clean, Cheat) are fully aligned and correct.
  - Checked folders, directories, and link references (e.g. `Saves/AkronHudLabels`); confirmed they are completely accurate.
  - No changes needed; marked as reviewed.
- Reviewed `docs/feature-guide/level.mdx`:
  - Verified level-scoped tools, automation, freezing, proof helpers, visual suppression, and hitbox display options against `AkronModuleSettings.cs`, `AkronFeatureRegistry.cs`, `AkronPolicy.cs`, and `akron-overlay-entries.cs`.
  - Verified all 45 main option toggles and their sub-menu option details (such as hitboxes, auto kill, auto deafen, lag pauser, screen tint, show hitboxes on death, etc.).
  - Confirmed that attempt classifications (Cheat, Regular clean, Goldberry/Hardlist clean) match registry definitions exactly.
  - No changes needed; marked as reviewed.

## 2026-05-26 21:13:00
- Reviewed `docs/feature-guide/player.mdx`:
  - Verified player-scoped options, assist options, presentation tools, trail features, invincibility, and resources (Dash Bar, Stamina Bar, etc.) against C# source files `akron-overlay-entries.cs`, `akron-overlay-madeline-popups.cs`, `akron-overlay-noclip-popups.cs`, and `akron-overlay-resource-popups.cs`.
  - Identified multiple stale and incorrect sub-option mappings in the documentation:
    - `Air Jumps`: Replaced `Infinite` with `Infinite jumps` to match exact ImGui label.
    - `Click Teleport` and `Golden Start`: Removed from the "Option menus" table since they are simple direct actions and do not have an expandable options submenu (no triangle in the overlay UI).
    - `Dash Number` and `Speed Number`: Removed non-existent `Offset X` and `Color` options; corrected to `Offset Y`, `Opacity`, `Text` (color), and `Outline` to match `DrawDashNumberPopupControls` and `DrawSpeedNumberPopupControls` exactly.
    - `Frame Stepper`: Removed `Freeze` from option menus; renamed `Hold repeat` to `Hold key repeats` and `Interval` to `Every` to match the exact C# code labels and tooltips.
    - `Grab Mode`: Updated descriptions for `Toggle` and `Invert` options to match the exact, detailed tooltips implemented in C#.
    - `Hazard Accuracy`: Added all missing options that are drawn in `DrawHazardAccuracyPopupControls` but were completely missing from the doc: `Invalid limit`, `Screen tint` (labeled `Tint` in table), `On invalid entry`, `While touching` (radio modes), `Tint color`, `Tint opacity`, `Tint duration`, `Reset Accuracy` and `Defaults` buttons.
    - `Madeline Colors`: Added missing `Speed` (gradient speed), `No dash`, `One dash`, `Two dash`, `Three dash`, `Four dash`, `Five dash` color picker options, and `Gradient A` and `Gradient B` to fully cover `DrawMadelineColorsPopupControls`.
    - `Noclip`: Renamed `Draw on top` and `Hide player` to their exact ImGui labels: `Draw Madeline on top` and `Hide Madeline`.
  - Stage, committed, and pushed both `docs/feature-guide/player.mdx` and the updated progress JSON to main.
